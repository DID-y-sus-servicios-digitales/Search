name: DeepSeek

on: [push]

jobs:
  deepseek:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Ejecutar script DeepSeek
      run: ./deepseek_script.sh "Hola, DeepSeek"
